"use client";
import { GeneralPrepPanel } from "./general-prep/GeneralPrepPanel";

// /interview — default landing: general (non-company-specific) prep.
export default function InterviewGeneralPage() {
  return <GeneralPrepPanel />;
}
